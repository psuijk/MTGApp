<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             mc:Ignorable="d"
             x:Class="SampleApp.Views.PlayerLifeView"
             x:Name="PlayerLifeContentView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <ScrollView x:Name="Flyout" Orientation="Horizontal" VerticalOptions="FillAndExpand" BackgroundColor="LightPink">
                <Label Text="FOR the most wild, yet most homely narrative which I am about to pen, I neither expect nor solicit belief. Mad indeed would I be to expect it, in a case where my very senses reject their own evidence. Yet, mad am I not -- and very surely do I not dream. But to-morrow I die, and to-day I would unburthen my soul. My immediate purpose is to place before the world, plainly, succinctly, and without comment, a series of mere household events. In their consequences, these events have terrified -- have tortured -- have destroyed me. Yet I will not attempt to expound them. To me, they have presented little but Horror -- to many they will seem less terrible than barroques. Hereafter, perhaps, some intellect may be found which will reduce my phantasm to the common-place -- some intellect more calm, more logical, and far less excitable than my own, which will perceive, in the circumstances I detail with awe, nothing more than an ordinary succession of very natural causes and effects." >
                </Label>
            </ScrollView>
        </Grid>
        <Grid x:Name="CustomMainContent" ColumnSpacing="0" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <Label x:Name="LifeTotalLabel"
                Grid.RowSpan="2"
                Grid.Row="0" 
                Grid.Column="0" 
                Grid.ColumnSpan="2"
                HorizontalTextAlignment="Center" 
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" 
                FontSize="120"
                TextColor="White"/>
            <BoxView 
                x:Name="DecrementBox"
                Grid.Row="1"
                Grid.Column="0" 
                Grid.ColumnSpan="2"
                Opacity="0">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer x:Name="DecrementTapGestureRecognizer"/>
                    <PanGestureRecognizer PanUpdated="OnPanUpdated" />
                </BoxView.GestureRecognizers>
            </BoxView>
            <BoxView
                x:Name="IncrementBox"
                Grid.Row="0"
                Grid.Column="0" 
                Grid.ColumnSpan="2"
                Opacity="0">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer x:Name="IncrementTapGestureRecognizer"/>
                    <PanGestureRecognizer PanUpdated="OnPanUpdated" />
                </BoxView.GestureRecognizers>
            </BoxView>
        </Grid>
    </Grid>
</ContentView>